import 'package:flutter/material.dart';

class DetalleTurnos extends StatelessWidget {
  const DetalleTurnos({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
