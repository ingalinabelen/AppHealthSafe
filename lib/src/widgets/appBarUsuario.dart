import 'package:flutter/material.dart';

Widget appBarUsuario(BuildContext context) {
  return AppBar(
    actions: <Widget>[
      IconButton(icon: Icon(Icons.email_outlined), onPressed: () => {})
    ],
  );
}
